[project]
name = "solomon"
version = "0.1.0"
description = "Solomon Autonomous Intelligence Platform"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["solomon*"]
exclude = ["app*", "dinosaurs*"]

[project.scripts]
solomon = "solomon.main:main"
